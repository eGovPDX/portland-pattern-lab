<div class="card">
  <div class="card-body">
    {% if title %}<h5 class="card-title">{{ title }}</h5>{% endif %}
    {% if text %}<p class="card-text">{{ text }}</p>{% endif %}
    {% if cta %}<a href="{{ cta.url }}" class="btn btn-primary">{{ cta.text }}</a>{% endif %}
  </div>
</div>
